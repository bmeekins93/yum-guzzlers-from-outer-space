// Main game file
const config = {
  type: Phaser.AUTO,
  width: 1280,
  height: 720,
  backgroundColor: '#000000',
  scene: {
    preload: function () {},
    create: function () {},
    update: function () {}
  }
};

const game = new Phaser.Game(config);
